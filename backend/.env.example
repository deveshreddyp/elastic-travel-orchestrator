# ─── Weather (only external API key needed) ──────────────────────
OPENWEATHER_API_KEY=
OPENWEATHER_CITY_ID=5391959

# ─── Map Routing (no key — uses OSRM public server) ─────────────
OSRM_BASE_URL=https://router.project-osrm.org
NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org

# ─── Redis ───────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ─── Mock APIs (local — zero external dependency) ────────────────
MOCK_TRANSIT_URL=http://localhost:4001
MOCK_EBIKE_URL=http://localhost:4001

# ─── Demo Mode ───────────────────────────────────────────────────
DEMO_MODE=true
DEMO_SESSION_ID=demo-maya-001
